/**
 * Find the most common character in a given string.
 * @param {string} str - The string to tested.
 * @returns {string} The most common character.
 */
function mostCommonChar(str) {}

module.exports = mostCommonChar;
