/**
 * Reverse an integer while retaining its sign (positive/negative).
 * @param {number} n - The integer to be reversed.
 * @returns {number} The reversed integer
 */
function reverseInteger(n) {}

module.exports = reverseInteger;
